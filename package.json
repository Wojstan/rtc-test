{
  "name": "mysterious-crawler",
  "version": "1.0.0",
  "description": "SR",
  "main": "app.js",
  "scripts": {
    "lint:check": "pnpm biome check .",
    "lint:fix": "pnpm lint:check --write",
    "start": "node dist/app.js",
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run --coverage"
  },
  "author": "SR",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@vitest/coverage-v8": "^1.3.1",
    "@types/express": "^5.0.1",
    "typescript": "^5.4.2",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
